{
  "name": "playlist",
  "version": "1.0.0",
  "description": "control the client and the server",
  "main": "index.js",
  "scripts": {
    "start": "cd server && node index.js",
    "install": "(cd client && npm install && ng build) && cd.. && cd server && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "meir shtiglitz",
  "license": "ISC"
}
